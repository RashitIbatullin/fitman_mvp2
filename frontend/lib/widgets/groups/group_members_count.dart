import 'package:flutter/material.dart';

class GroupMembersCount extends StatelessWidget {
  const GroupMembersCount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('0 members');
  }
}
