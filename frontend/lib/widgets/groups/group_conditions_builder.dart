import 'package:flutter/material.dart';

class GroupConditionsBuilder extends StatelessWidget {
  const GroupConditionsBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Padding(
        padding: EdgeInsets.all(16.0),
        child: Text('Group Conditions Builder'),
      ),
    );
  }
}
