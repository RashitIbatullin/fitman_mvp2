import 'package:flutter/material.dart';

class AutoGroupIndicator extends StatelessWidget {
  const AutoGroupIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.autorenew, size: 16);
  }
}
