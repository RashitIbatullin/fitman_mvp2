import 'package:flutter/material.dart';

class GroupTypeBadge extends StatelessWidget {
  const GroupTypeBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Chip(
      label: Text('Group Type'),
    );
  }
}
